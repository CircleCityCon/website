box: debian
build:
  steps:
    - arjen/hugo-build@1.23.2:
      version: "0.30.2"
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - kwyjibo/gh-pages@0.2.4:
        token: $GIT_TOKEN
        domain: circlecitycon.com
        branch: gh-pages
        basedir: public
  after-steps:
      - slack-notifier:
          url: $SLACK_URL
          channel: webops
          username: werckerbot